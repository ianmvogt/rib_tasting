# Blind Rib Tasting App

A Streamlit app for conducting blind rib tastings with multiple judges.

## Features
- Score 5 sets of ribs across 5 categories
- Real-time results visualization
- Export data as JSON

## Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
```